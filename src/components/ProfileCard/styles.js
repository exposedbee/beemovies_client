import {makeStyles} from "@material-ui/core";

export const useStyles = makeStyles({
   root:{
      backgroundColor : '#757ce8',
      height : '100%',
   },
   profileCard :{
      marginTop : 60,
   } ,
   detail : {
      padding:20,
   }
});